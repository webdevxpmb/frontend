import styled from 'styled-components';

export const Link = styled.a`
  	clear: both;
    position: relative;
    z-index: 10;
    margin-top: 2em;
    color: pink;

`;

export const Name = styled.p`
    font-weight:800;
    text-transform:uppercase;
    letter-spacing:0.05em;
    margin-top: 1em;
    margin-bottom: 0.5em;
    margin-right: 1em;
    margin-left: 2em;
    display: inline-block;
`;


export const ShortPar = styled.p`
  	font-weight:400;
    letter-spacing:0.05em;
    margin-right: 1em;
    margin-left: 2em;
    margin-top: 0.5em;
    margin-bottom: 0.5em;
`;
