// import { fromJS } from 'immutable';
// import { makeSelectAnnouncementPageDomain } from '../selectors';

// const selector = makeSelectAnnouncementPageDomain();

describe('makeSelectAnnouncementPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
