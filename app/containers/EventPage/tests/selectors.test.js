// import { fromJS } from 'immutable';
// import { makeSelectEventPageDomain } from '../selectors';

// const selector = makeSelectEventPageDomain();

describe('makeSelectEventPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
