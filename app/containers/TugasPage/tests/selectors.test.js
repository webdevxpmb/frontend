// import { fromJS } from 'immutable';
// import { makeSelectTugasPageDomain } from '../selectors';

// const selector = makeSelectTugasPageDomain();

describe('makeSelectTugasPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
