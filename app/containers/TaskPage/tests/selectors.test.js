// import { fromJS } from 'immutable';
// import { makeSelectTaskPageDomain } from '../selectors';

// const selector = makeSelectTaskPageDomain();

describe('makeSelectTaskPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
